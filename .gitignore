# Age private keys (never commit)
*.agekey
age.key

# Cluster overlays (private deployment data — lives in fleet-deploy repo)
clusters/

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
tfplan

# OS
.DS_Store
